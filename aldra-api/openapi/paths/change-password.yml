post:
  summary: Change Password
  description: Change Password
  operationId: execute
  tags:
    - ChangePassword
  security:
    - Bearer: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/ChangePasswordRequest.yml"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/ChangePasswordResponse.yml"
