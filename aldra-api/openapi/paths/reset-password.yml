post:
  summary: Reset Password
  description: Reset Password
  operationId: execute
  tags:
    - ResetPassword
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/ResetPasswordRequest.yml"
  responses:
    200:
      description: OK
