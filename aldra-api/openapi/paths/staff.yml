post:
  summary: Create staff
  description: Create a staff
  operationId: execute
  tags:
    - CreateStaff
  security:
    - Bearer: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/CreateStaffRequest.yml"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/CreateStaffResponse.yml"

put:
  summary: Update staff
  description: Update a staff
  operationId: execute
  tags:
    - UpdateStaff
  security:
    - Bearer: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/UpdateStaffRequest.yml"
  responses:
    200:
      description: OK
