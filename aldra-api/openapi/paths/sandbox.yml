get:
  summary: list sandbox
  description: get a list of sandbox
  operationId: execute
  tags:
    - GetSandbox
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/GetSandboxResponse.yml"

post:
  summary: create sandbox
  description: create a sandbox
  operationId: execute
  tags:
    - CreateSandbox
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/CreateSandboxRequest.yml"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/CreateSandboxResponse.yml"
